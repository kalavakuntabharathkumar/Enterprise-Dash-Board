The RBAC system is now stable and working correctly.

Now safely implement the FOUNDATION for a workflow approval engine focused on Leave Request approvals WITHOUT rewriting the existing architecture.

IMPORTANT:
Do NOT redesign the frontend.
Do NOT regenerate unrelated modules.
Do NOT rewrite RBAC.
Do NOT break existing APIs.
Do NOT create a complex BPM engine.
Do NOT overengineer.

Current system state:

* RBAC is implemented
* Permission middleware exists
* Frontend route guards exist
* Dynamic sidebar exists
* SQLite database is active
* FastAPI backend is modular

GOAL:
Build a clean enterprise workflow foundation for leave approvals.

WORKFLOW TARGET

Employee submits leave request
→ Department Head reviews
→ HR Manager final approval/rejection
→ Notifications generated
→ Workflow history logged

REQUIRED TASKS

1. Create workflow database tables.

Create:

leave_requests

* id
* employee_id
* leave_type
* start_date
* end_date
* reason
* status
* current_approver_role
* created_at
* updated_at

workflow_logs

* id
* entity_type
* entity_id
* action
* performed_by
* role
* comments
* timestamp

notifications

* id
* user_id
* title
* message
* type
* is_read
* created_at

2. Keep implementation modular.

Create:
backend/app/workflows/
backend/app/notifications/

Use clean separation:

* routes
* services
* schemas
* utils

3. Create basic workflow states.

Supported statuses:

* pending_department
* pending_hr
* approved
* rejected

4. Create workflow transition logic.

Rules:

* Employee can create leave request
* Department Head can approve/reject pending_department
* HR Manager can approve/reject pending_hr

5. Create notification generation logic.

Examples:

* "New leave request submitted"
* "Leave request approved"
* "Leave request rejected"

6. Create workflow log entries automatically.

Every workflow action should create a log entry.

Examples:

* submitted
* approved
* rejected

7. Keep backend APIs permission protected.

Use existing RBAC middleware.

Examples:
require_permission("approve_leave")

8. Preserve existing architecture.

Do NOT:

* rewrite auth
* rewrite sidebar
* rewrite frontend routing
* modify unrelated modules

9. Keep SQLite compatibility.
10. Keep Replit compatibility.

IMPORTANT IMPLEMENTATION RULES

* Keep workflow engine lightweight and modular
* Avoid giant files
* Avoid hardcoded role logic everywhere
* Reuse existing RBAC utilities
* Keep business logic centralized
* Build only Leave Workflow foundation for now

BEFORE MODIFYING FILES:
First analyze:

* existing HRMS module structure
* safest integration points
* how workflow APIs should connect with RBAC
* how notifications should remain modular

Then implement incrementally and safely.
